{
  "name": "crucible-mcp-server",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Crucible MCP server — controlled, allowlisted access to the self-owned CTF arena.",
  "scripts": {
    "typecheck": "tsc -p tsconfig.json",
    "test": "tsx --test test/*.test.ts",
    "start": "node --import tsx src/index.ts",
    "start:http": "node --import tsx src/http.ts"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.30.0",
    "zod": "^4.0.0"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "tsx": "^4.19.0",
    "typescript": "^5.6.0"
  }
}
